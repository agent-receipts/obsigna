# Alloy distribution jar — fetched on demand by run.sh from Maven Central.
# Never commit the 21 MB binary.
alloy.jar
*.jar

# Alloy `exec` output (solutions + receipt.json), regenerated on every run.
out/
